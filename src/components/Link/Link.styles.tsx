import { StyleSheet } from "react-native";
import { colors } from "../../styles/global.styles";

const styles = StyleSheet.create({
  container: { borderRadius: 15 },
  url: { color: colors.link },
});

export default styles;
